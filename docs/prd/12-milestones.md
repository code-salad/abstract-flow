# 12. Milestones (assumption — adjust to your capacity)

1. **M0 – Spike (2 wks):** `core/cfg.ts` + `core/resolve.ts` against the TS compiler provider on 3 real OSS repos; measure resolution rate. Kill criterion: < 90% resolution on conventional Express codebases.
2. **M1 – Core engine (4 wks):** DBOS indexing pipeline, SQLite index, lazy CFG cache, labeling chain, noise filter; `/v1` API + OpenAPI + generated client.
3. **M2 – Flow UI (4 wks):** flow canvas with custom layered layout, expand/focus navigation, breadcrumb, code peek, editor deep-link.
4. **M3 – MVP polish (2 wks):** entry-point detection for Express, export/permalink, watch mode, onboarding, private beta with 10 users.
